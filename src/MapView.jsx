import React from 'react'; import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet'; export default function MapView({markers=[], center=null}){ const start = center || (markers.length>0 && markers[0].coords) || {lat:-27.1,lng:-52.616}; return (<MapContainer center={[start.lat,start.lng]} zoom={12} style={{height:'60vh',width:'100%'}}><TileLayer url='https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png' attribution='&copy; OpenStreetMap'/> {markers.map(m=>m.coords? (<Marker key={m.id} position={[m.coords.lat,m.coords.lng]}><Popup><div><strong>{m.nome}</strong><br/>{m.categoria}</div></Popup></Marker>):null)} </MapContainer>); }
